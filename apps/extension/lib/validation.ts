// Re-exported from the shared schema module (valibot).
// Kept as a module boundary so extension imports stay stable.
export {
  viewerOriginSchema,
  expiryModeSchema,
  validateViewerOrigin,
  validateExpiryMode,
} from "@stash/shared";
