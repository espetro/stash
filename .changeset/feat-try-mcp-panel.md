---
"stash-extension": minor
---

Add a "Try MCP" panel to the extension options page. Connects to the background MCP server, lists tools, lets the user call one and see the response. Dogfoods the previously unused `connectToBackgroundMcp()`.