{
  "name": "tab-mail",
  "private": true,
  "scripts": {
    "dev:ext": "pnpm --filter tab-mail-extension run dev:firefox",
    "dev:view": "pnpm --filter tab-mail-viewer run dev",
    "build": "turbo run build",
    "screenshots": "tsx scripts/generate-screenshots.ts"
  },
  "devDependencies": {
    "playwright": "^1.58.2",
    "tsx": "^4.21.0",
    "turbo": "^2.8.11"
  },
  "packageManager": "pnpm@10.26.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@getgauge/cli",
      "esbuild",
      "protobufjs",
      "sharp",
      "spawn-sync"
    ]
  }
}
